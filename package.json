{
  "name": "neuint-site",
  "version": "0.1.1",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
